重构响应

上一个版本实现了响应正确页面和404页面。但是代码有重复，因此我们需要重构发送响应的部分

响应的格式与请求的格式类似，因此我们也可以在设计一个类：HttpResponse，使用这个类的实例来表示一个
具体要发送给客户端的响应内容。然后统一方式发送

实现：
	1.在com.webserver.http包中新建类：HttpResponse
	2.定义flush方法，用户发送当前响应内容
	3.在ClientHandler中先使用这个对象测试响应正确资源的情况
	
