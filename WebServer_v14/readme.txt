解决空请求问题

Http协议允许客户端发送空请求：客户端连接后没有发送任何内容而是直接与客户端断开了连接。
此时我们服务端若按照标准流程解析时请求时，在读取请求行后获取其中三部分时就会出现下标越界，导致
后续操作出现其他的异常。

对于空请求而言，我们接受到应当直接忽略所有操作，直接与客户端断开连接即可。

思路：
	当解析请求的请求行时，如果遇到空请求，则抛出一个异常给ClientHandler,使其忽略后续一切操作，直接
	与客户端断开连接即可。